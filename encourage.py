"""Encouragement helpers."""


def encourage(topic: str) -> str:
    """Return an encouraging message about the given topic.

    Args:
        topic: The topic to encourage the reader about.

    Returns:
        A message of the form ``'Keep going with <topic>!'``.
    """
    return f"Keep going with {topic}!"
