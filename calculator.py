"""Minimal arithmetic helpers for cohort CU-02."""

__all__ = ["add", "mul"]


def add(a: float, b: float) -> float:
    """Return the sum of ``a`` and ``b``."""
    return a + b


def mul(a: float, b: float) -> float:
    """Return the product of ``a`` and ``b``."""
    return a * b
